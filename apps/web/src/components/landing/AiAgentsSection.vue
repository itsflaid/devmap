<script setup lang="ts">
// AI agent compatibility and context-flow data.
</script>

<template>
  <section id="ai-agents" class="landing-section ai-agents-section">
    <!--
      FOR AI AGENTS SECTION

      Headline:
      "Your AI agent is only as good as its context."

      Copy:
      "Without DevMap, your AI agent explores your codebase blindly -
      opening files one by one, guessing what matters, rebuilding
      context from scratch every session.

      With DevMap, your agent gets a ready-made project map.
      Framework, entry points, critical files, data flow - all structured,
      all accurate, all generated from static analysis."

      Command flow:
      "devmap analyze
      -> generates .devmap/snapshot.json
      -> generates DEVMAP.md"

      Closing copy:
      "Your AI agent reads the map. Not the entire codebase.
      Faster execution. Lower token usage. Better results."

      Compatible with:
      - Claude Code
      - OpenAI Codex
      - Gemini CLI
      - Cursor
      - GitHub Copilot

      Pesan inti:
      AI agent membutuhkan peta project, bukan eksplorasi codebase secara buta.
    -->
  </section>
</template>

<style scoped>
/* AI agents section styles. */
</style>
